[Service]
# Docker rebuilds the DOCKER-USER chain on startup; re-apply our rules right away so the
# kill switch has no gap. (install.sh places this under /etc/systemd/system/docker.service.d/ when Docker is present.)
ExecStartPost=-/opt/ap-vpn/bin/ap-firewall.sh
